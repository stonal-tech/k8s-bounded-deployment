---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: boundeddeployments.deploy.stonal.io
spec:
  group: deploy.stonal.io
  names:
    kind: BoundedDeployment
    listKind: BoundedDeploymentList
    plural: boundeddeployments
    singular: boundeddeployment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BoundedDeployment is the Schema for the boundeddeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BoundedDeploymentSpec defines the desired state of BoundedDeployment
            properties:
              margin:
                description: Margin between min & max
                type: integer
              maxReplicas:
                description: Maximum number of replicas to for the deployment (optional)
                type: integer
              replicas:
                description: Minimum number of replicas to for the deployment
                minimum: 0
                type: integer
              sourceDeploymentName:
                description: Name of the source deployment to copy the template from
                type: string
              template:
                description: Template for the pods to start
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            description: BoundedDeploymentStatus defines the observed state of BoundedDeployment
            properties:
              nbPodsCreated:
                type: integer
              nbPodsDeleted:
                type: integer
              nbPodsOutdated:
                type: integer
              replicas:
                description: Current number of replicas
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
