---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: boundeddeployments.deploy.stonal.io
spec:
  group: deploy.stonal.io
  names:
    kind: BoundedDeployment
    listKind: BoundedDeploymentList
    plural: boundeddeployments
    singular: boundeddeployment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BoundedDeployment is the Schema for the boundeddeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BoundedDeploymentSpec defines the desired state of BoundedDeployment
            properties:
              margin:
                description: Margin between min & max
                type: integer
              maxReplicas:
                description: Maximum number of replicas to for the deployment (optional)
                type: integer
              replicas:
                description: Minimum number of replicas to for the deployment
                minimum: 0
                type: integer
              template:
                description: Template for the pods to start
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            description: BoundedDeploymentStatus defines the observed state of BoundedDeployment
            properties:
              readyReplicas:
                description: Current number of ready replicas
                type: integer
              replicas:
                description: Current number of replicas
                type: integer
              templateHash:
                description: template hash
                type: string
            type: object
        type: object
    served: true
    storage: true
    additionalPrinterColumns:
      # ─── Spec fields ───────────────────────────────────────────────
      - name: Min
        type: integer
        description: Minimum desired replicas
        jsonPath: .spec.replicas
      - name: Max
        type: integer
        description: Maximum allowed replicas (optional)
        jsonPath: .spec.maxReplicas
      - name: Margin
        type: integer
        description: Margin between min & max
        jsonPath: .spec.margin

      # ─── Status fields ─────────────────────────────────────────────
      - name: Current
        type: integer
        description: Observed replicas
        jsonPath: .status.replicas
      - name: Ready
        type: integer
        description: Ready replicas
        jsonPath: .status.readyReplicas

      # ─── Other useful info (hidden on narrow terminals) ────────────
      - name: Hash
        type: string
        description: Pod template hash
        jsonPath: .status.templateHash
        priority: 1        # show only with “-o wide”
    subresources:
      status: {}
