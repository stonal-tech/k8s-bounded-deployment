---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: mindeployments.deploy.stonal.io
spec:
  group: deploy.stonal.io
  names:
    kind: MinDeployment
    listKind: MinDeploymentList
    plural: mindeployments
    singular: mindeployment
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MinDeployment is the Schema for the mindeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MinDeploymentSpec defines the desired state of MinDeployment
            properties:
              maxReplicas:
                description: Maximum number of replicas to for the deployment (optional)
                type: integer
              replicas:
                description: Minimum number of replicas to for the deployment
                minimum: 1
                type: integer
              sourceDeploymentName:
                description: Name of the source deployment to copy the template from
                type: string
              template:
                description: Template for the pods to start
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            description: MinDeploymentStatus defines the observed state of MinDeployment
            properties:
              nbPodsCreated:
                type: integer
              nbPodsDeleted:
                type: integer
              replicas:
                description: Current number of replicas
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
